export enum SortMethod {
  Ascending = '1',
  Descending = '2',
}

export enum MassOpts {
  Allowed = 'true',
  Disallowed = 'false',
}

export enum ListDisplayMode {
  List = '1',
  Calendar = '2',
  Timeline = '3',
}
