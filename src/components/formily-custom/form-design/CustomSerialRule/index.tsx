import React from 'react';
import { ISchemaFieldComponentProps } from '@formily/antd';

// import styles from './index.scss';
/**
 * 自定义 custom-serial-rule
 * 用于流水号规则设置
 */

// interface RuleProps {
//   Type: number;
//   Value?: string;
//   IncreNum?: number;
//   Label?: string;
//   Reset?: boolean;
// }
// enum

const CustomSerialRule: React.FC<ISchemaFieldComponentProps> = () => (
  //   const { value } = props;
  <>
    {/* <div className={styles.ruleTitle}>
      流水号规则 <a>设置</a>
    </div>
    <div>提交日期：年</div>
    <div>自增位数：8位，不重置</div> */}
    This feature is under development, so stay tuned!
  </>
);
export default CustomSerialRule;
